# Bedrock Cost Keeper - Environment Configuration Example
# Copy this file to .env and update with your values

# Service Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=false

# AWS Configuration
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=123456789012

# DynamoDB Local endpoint (Secrets Manager and Bedrock still use real AWS)
DYNAMODB_ENDPOINT_URL=http://localhost:8000

# CORS Configuration (comma-separated list of allowed origins)
# Leave empty for development defaults (localhost:3000, localhost:8000)
CORS_ALLOWED_ORIGINS=https://<app.example.com>,https://<app2.example.com>

# Authentication - Secrets Manager
# Secrets will be fetched from AWS Secrets Manager at startup
JWT_SECRET_NAME=bedrock-cost-keeper/dev/jwt-secret
PROVISIONING_API_KEY_NAME=bedrock-cost-keeper/dev/provisioning-api-key

# Environment
ENVIRONMENT=dev
